# v2 is a static build — these are the ONLY deps the build chain imports.
# (No runtime server: flask/gunicorn/werkzeug/livereload were v1-only and were
#  dropped in M14a. The v1 Flask app is archived at archive/app-flask-v1.py.)
pandas>=2.0          # data_processor.py — CSV → JSON precompute
jinja2>=3.0          # build_static.py — renders index.html (was pulled in via flask before)

# build-time asset rendering (build_assets.py)
cairosvg>=2.7        # SVG icons → PNG
Pillow>=10.0         # PNG resize / ICO
